version: '3.8'

services:
  plumber-api:
    build:
      context: .
      dockerfile: Dockerfile.api
    ports:
      - "33857:33857"
    restart: unless-stopped
    develop:
      watch:
        - action: sync+restart
          path: ./plumber.R
          target: /api/plumber.R
